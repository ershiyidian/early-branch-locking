# Contributing

Keep changes reproducible and scoped to the public research code. Run the smoke
test and `python -m compileall` before submitting a change. Do not add model
weights, generated results, credentials, or internal manuscript material.
